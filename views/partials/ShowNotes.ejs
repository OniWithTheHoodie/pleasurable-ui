<ul class="show_notes">
    <% if (feedback.length > 0) { %>
        <% if (feedback[feedback.length - 1].note) { %>
            <li> <%=(feedback[feedback.length - 1].note) %></li>
        <% }


        else { %>
            <li>Er is geen notitie.</li>
        <% } %>
    <% }

    else { %>
        <li>No feedback available.</li>
    <% } %>
</ul>

