<ul class="show_notes">
    <% if (feedback) { %>


        <% feedback.forEach(function(item, index) { %>
            <p>Item: <%= item.note %></p>
    <% })

    } else { %>
        <li>No feedback available.</li>

    <% } %>
</ul>

